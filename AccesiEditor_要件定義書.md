# 要件定義書

**プロジェクト名：** AccessiEditor（仮称）  
**作成日：** 2026年5月13日  
**対象：** 高校情報I 自由研究用Webアプリ  
**作成者：** Thomas（担当教員）

---

## 1. プロジェクト概要

### 背景

iPad Pro（第4世代）のみの環境しか持たない生徒が、視覚障害者に優しいWebページを自由研究として開発する。PCや海外Webサービスに依存しない、Safari単体で完結するHTMLエディタ兼アクセシビリティチェックツールを教員側が構築・提供する。

### 目的

- 生徒がiPad SafariのみでリアルタイムにHTMLを書き・確認できる環境を提供する
- アクセシビリティの学習と実践を同一ツール上で完結させる
- コーディング初心者でも使えるノーコードモードを併用する
- AIチャット機能により、詰まったときにその場で相談できる環境を提供する

---

## 2. 技術スタック

| 項目 | 選定 | 理由 |
|------|------|------|
| 言語 | HTML / CSS / Vanilla JS | 外部依存ゼロ・Safari完全対応 |
| フレームワーク | なし（単一HTMLファイル） | インストール不要・オフライン動作可 |
| エディタライブラリ | CodeMirror 6（CDN） | iPad Safariで動作実績あり |
| AIチャット | Gemini API（`gemini-2.0-flash`） | 無料枠が十分・日本語対応 |
| 配布方法 | 単一`.html`ファイル | AirDrop・Google Classroom等で配布可能 |
| 外部通信 | Gemini APIのみ | 海外Webサービス不使用・CDN初回読込のみ |

> **重要：** 単一HTMLファイルとして完結させる。生徒はファイルをSafariで開くだけで使用可能。

---

## 3. 対象ユーザー

| ユーザー | 概要 |
|----------|------|
| 主ユーザー | 高校1年生（HTML初心者・iPad Pro 第4世代使用） |
| 副ユーザー | 担当教員（進捗確認・評価用途） |

---

## 4. 機能要件

### 4.1 コアレイアウト

```
┌─────────────────────────────────────────┐
│  ヘッダー（アプリ名・モード切替・操作ボタン）  │
├────────────────┬────────────────────────┤
│                │                        │
│  エディタ       │  プレビュー             │
│  （左ペイン）   │  （右ペイン）           │
│                │                        │
├────────────────┴────────────────────────┤
│  アクセシビリティチェックパネル（下部）         │
└──────────────────────────── [💬] ────────┘
                               ↑ 右下固定
```

- iPad **横向き**：左右分割（エディタ｜プレビュー）
- iPad **縦向き**：上下分割（エディタ→プレビュー）
- チャットボタンは常時右下に固定表示

---

### 4.2 モード切替

#### モードA：コードエディタモード

- CodeMirrorベースのHTMLエディタ
- シンタックスハイライト（HTML / CSS / JS）
- 入力と同時にプレビューをリアルタイム更新（`iframe` + `srcdoc`）
- Undo / Redo 対応
- テンプレート挿入ボタン（4.3参照）

#### モードB：ノーコードモード

- GUIパネルから要素を選んでHTMLを組み立てる
- 追加できる要素：見出し・段落・画像・リンク・リスト・テーブル
- 各要素の編集はモーダルダイアログで行う
- バックグラウンドでHTMLを自動生成し、プレビューに反映
- 「コードを見る」ボタンでモードAに切替・生成コードを確認可能

#### モード共通

- モードAとモードBは同一HTMLを共有（切替時にデータ引継ぎ）
- `localStorage`にHTMLを自動保存（Safari終了後も復元）

---

### 4.3 テンプレート機能

以下のテンプレートをワンタップで挿入できる：

| テンプレートID | 内容 |
|--------------|------|
| `tpl-blank` | 空のHTMLボイラープレート（`lang="ja"` / viewport設定済） |
| `tpl-accessible-basic` | アクセシビリティ基本構造（skip link・landmark・alt付き画像） |
| `tpl-color-safe` | CUD配色済みの基本ページ（コントラスト比4.5:1以上保証） |
| `tpl-form` | アクセシブルなフォーム（`label` / `aria-required`付き） |

---

### 4.4 アクセシビリティチェック機能（必須）

チェックは「チェックボタン押下時」に実行。結果はチェックパネルに一覧表示。

#### チェック項目一覧

| ID | チェック内容 | 重大度 |
|----|------------|--------|
| `A01` | `<img>`タグに`alt`属性があるか | 🔴 エラー |
| `A02` | `alt`属性が空文字（`alt=""`）になっていないか（装飾画像を除く） | 🟡 警告 |
| `A03` | `<h1>`が1つ以上存在するか | 🔴 エラー |
| `A04` | 見出しレベルがスキップされていないか（h1→h3など） | 🟡 警告 |
| `A05` | `<html lang="ja">`が設定されているか | 🔴 エラー |
| `A06` | `<title>`タグが設定されているか | 🔴 エラー |
| `A07` | `<a>`タグのテキストが「こちら」「クリック」になっていないか | 🟡 警告 |
| `A08` | `<form>`内の`<input>`に対応する`<label>`があるか | 🔴 エラー |
| `A09` | `<table>`に`<caption>`があるか | 🟡 警告 |
| `A10` | `blink` / `marquee`タグが使われていないか | 🔴 エラー |
| `A11` | テキストと背景のコントラスト比がWCAG AA基準を満たすか | 🔴 エラー |

#### コントラスト比チェック（A11）詳細

- プレビュー内のテキスト要素を対象に`getComputedStyle()`で前景色・背景色を取得
- WCAG AA基準（通常テキスト 4.5:1 以上 / 大テキスト 3:1 以上）と照合
- 基準未満の要素をプレビュー上でハイライト表示
- チェックパネルに「要素名・実測値・基準値」を表示

#### チェック結果の表示形式

```
🔴 エラー 3件　🟡 警告 2件　✅ 合格 6件
────────────────────────────────────────
🔴 [A01] <img>にalt属性がありません（3箇所）
🔴 [A05] <html>にlang属性がありません
🟡 [A07] リンクテキスト「こちら」は具体的な説明に変更しましょう
```

---

### 4.5 ヒント表示機能

- エラー・警告の各項目をタップすると「なぜ問題か」「どう直すか」の解説が展開表示
- 解説文は高校生向けの平易な日本語で記述
- 「修正例を挿入」ボタンで正しいコードのスニペットをエディタに挿入（オプション）

---

### 4.6 その他機能

| 機能 | 詳細 |
|------|------|
| HTMLエクスポート | 現在のHTMLをファイルとしてダウンロード（`Blob` + `<a download>`） |
| プレビューリセット | プレビューをリロード |
| フォントサイズ調整 | エディタの文字サイズを3段階で切替（小・中・大） |
| ダークモード | OS設定に追従（`prefers-color-scheme`） |
| 自動保存通知 | 保存時に画面下部にトースト表示（「保存しました」） |

---

### 4.7 AIアシスタント機能（相談チャット）

#### 概要

Gemini API を利用した、アクセシビリティに特化した相談チャット機能。生徒がコーディング中に疑問を感じた際、画面を離れることなくAIに質問できる。

#### APIキーの取得・設定方法

1. [Google AI Studio](https://aistudio.google.com/) にアクセス（Googleアカウント必要）
2. 「Get API key」→「Create API key」でキーを発行
3. アプリ初回起動時の設定画面でキーを入力・`localStorage`に保存
4. キーはアプリ内にのみ保存され、外部サーバーには送信されない

> ⚠️ **無料枠について：** Gemini 2.0 Flash は2026年5月時点で1日あたり1,500リクエスト・毎分15リクエストまで無料。生徒1人の研究用途では実質無制限で使用可能。

#### UI設計

- 画面右下に固定の **[💬 AIに相談]** ボタンを配置
- タップするとチャットドロワーが下からスライドアップ
- エディタ・プレビューは隠れず、ドロワーは画面下半分に展開

#### チャット機能の詳細

| 項目 | 仕様 |
|------|------|
| 使用モデル | `gemini-2.0-flash` |
| 会話履歴 | セッション内のみ保持（ページ更新でリセット） |
| システムプロンプト | アクセシビリティ特化・高校生向け日本語で回答するよう固定 |
| コンテキスト送信 | 現在のエディタのHTML（最大3,000文字）を自動添付 |
| ストリーミング | 対応（回答を逐次表示） |
| APIキー未設定時 | 設定画面へ誘導するメッセージを表示 |

#### システムプロンプト（固定）

```
あなたは高校生の自由研究をサポートするWebアクセシビリティの専門家AIです。
以下のルールを必ず守ってください。

【役割】
- 視覚障害者に優しいWebページ制作を学んでいる高校1年生を助ける
- HTML・CSS・アクセシビリティについて、わかりやすい日本語で答える

【回答スタイル】
- 専門用語は使う場合、必ず説明を添える
- コードを示す場合は短いサンプルを提示する
- 回答は簡潔に。長くなる場合は箇条書きを使う
- 「なぜそうするか」の理由を必ず伝える

【範囲外の質問】
- アクセシビリティ・HTML・CSSと無関係な質問には
  「この質問はWebページ制作と関係がないので答えられません」と返す

現在のエディタのHTMLコードは以下の通りです：
---
{CURRENT_HTML}
---
```

#### クイック質問ボタン（ビルトイン）

チャット欄上部にタップ可能なショートカットを表示：

```
[ alt属性って何？ ]  [ コントラスト比を直したい ]  [ この見出し構造は正しい？ ]
```

#### セキュリティ・プライバシー

| 項目 | 対応 |
|------|------|
| APIキーの保管 | `localStorage`のみ・画面上にはマスク表示（`••••••••`） |
| 送信データ | エディタのHTML + ユーザーの質問文のみ |
| 外部サーバー | Gemini API（`generativelanguage.googleapis.com`）のみ通信 |
| 個人情報 | 氏名・学籍番号等は送信しない旨を初回セットアップ時に明示 |

---

## 5. 非機能要件

| 項目 | 要件 |
|------|------|
| 動作環境 | iPad Pro 第4世代 / Safari 17以上 |
| オフライン動作 | 必須（CDN読込後・AIチャット非使用時はネット不要） |
| レスポンシブ | iPad縦横どちらでも使用可能 |
| タッチ操作 | ボタン・タップターゲットは最小44×44px（Apple HIG準拠） |
| パフォーマンス | プレビュー更新は入力後300msデバウンス処理 |
| 外部通信 | CodeMirror CDN初回読込 + Gemini APIのみ |
| データ保存 | `localStorage`のみ（サーバー不要） |
| 言語 | UI・エラーメッセージ・ヒントはすべて日本語 |

---

## 6. 画面設計（概要）

### 6.1 ヘッダー

```
[ 🌐 AccessiEditor ]  [ 📝 コード | 🖱 ノーコード ]  [ ▶ チェック ]  [ 💾 ]  [ ⚙️ ]
```

### 6.2 エディタペイン（モードA）

- CodeMirrorエディタ（HTML mode）
- 上部にテンプレートボタン群（横スクロール）
- フォントサイズ切替ボタン（右上）

### 6.3 プレビューペイン

- `<iframe srcdoc="...">` でサンドボックス表示
- 右上に「更新」「全画面」ボタン

### 6.4 チェックパネル（下部ドロワー）

- 「▶ チェック実行」ボタンで開閉
- エラー・警告・合格のサマリーバッジ
- 各項目タップで詳細・解説を展開

### 6.5 チャットドロワー（右下→下部スライドアップ）

- 会話履歴（スクロール可）
- クイック質問ボタン（3つ）
- テキスト入力欄 + 送信ボタン
- ストリーミング表示（回答が逐次流れる）

### 6.6 設定画面（モーダル）

- Gemini APIキー入力・保存・削除
- フォントサイズ設定
- localStorage使用状況の表示

---

## 7. 開発フェーズ

| フェーズ | 内容 | 優先度 |
|---------|------|--------|
| Phase 1 | HTMLエディタ＋リアルタイムプレビュー | 🔴 最優先 |
| Phase 2 | アクセシビリティチェック（A01〜A11） | 🔴 最優先 |
| Phase 3 | AIチャット機能（Gemini API） | 🔴 最優先 |
| Phase 4 | ノーコードモード | 🟡 高 |
| Phase 5 | テンプレート機能・ヒント解説 | 🟡 高 |
| Phase 6 | ダークモード・エクスポート・細部UX | 🟢 中 |

---

## 8. 制約・前提条件

- 生徒はHTML初心者であるため、エラーメッセージは必ず**日本語**で表示する
- `localStorage`はSafariのプライベートブラウズモードでは動作しないため、その旨を画面上に注記する
- CodeMirrorはCDN経由で読み込むため、**初回起動時はインターネット接続が必要**
- AIチャット使用時はインターネット接続が必要
- ファイルダウンロード（エクスポート）はSafariの仕様上、「ファイル」アプリへの保存となる
- 将来的なクラス全体への展開を想定し、コードはコメントを充実させてメンテナンスしやすい構造にする
- Gemini APIキーは生徒に見せても問題ない範囲での使用に留める（無料枠内）

---

## 9. 未決定事項

| 項目 | 選択肢 |
|------|--------|
| アプリ名 | AccessiEditor / AccessiLab / その他 |
| CodeMirrorのバージョン | v6推奨・v5（よりシンプル）も可 |
| ノーコードの対応要素範囲 | 基本6要素のみか、もう少し広げるか |
| チェック項目の追加 | ARIA landmark存在チェック等を追加するか |
| 生徒の保存方法 | localStorageのみか、テキストコピーも併用するか |
| APIキーの管理 | Thomasが1つのキーを配布するか、生徒が各自取得するか |
| チャット履歴の保存 | sessionStorageで会話を保持するか、毎回リセットするか |
| クイック質問の内容 | 追加・変更するか |
